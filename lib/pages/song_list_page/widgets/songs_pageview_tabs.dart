import 'package:flutter/material.dart';

class SongsPageViewTabs extends StatelessWidget {
  const SongsPageViewTabs({super.key});

  @override
  Widget build(BuildContext context) {
    return const Row(

    );
  }
}